# 女友手账 - 统一功能规格

## 1. 产品定位

「女友手账」是一款 AI 虚拟女友日记应用。

它的核心不是普通的日记工具，而是一个有独立人格、记忆感和情绪表达的角色：

- 女友会每天自动写日记
- 女友会生成 2-3 条语音留言
- 女友会根据记忆、日期、情绪和关系阶段来调整表达方式
- 用户可以像翻手账一样浏览这些内容

第一版先做 Web 交互版，后续再平滑迁移到 iOS。

## 2. 核心架构

### 双 Agent 设计

#### 女友 Agent

前台对用户可见，负责输出最终内容。

职责：

- 写日记
- 发语音留言
- 根据记忆和情绪自然表达
- 保持统一的人格设定

人格特征：

- 温柔治愈
- 说话直接坦诚
- 偶尔撒娇，但不过度甜腻
- 有自己的情绪和小脾气

#### 恋爱军师 Agent

后台隐藏，对用户不可见。

职责：

- 分析当天适合什么情绪基调
- 选择日记切入角度
- 提炼情感钩子
- 生成结构化剧本，交给女友 Agent 执行

### 剧本格式

```json
{
  "angle": "切入角度",
  "emotional_tone": "sweet | miss | playful | touched",
  "hooks": ["hook1", "hook2"],
  "memory_activated": "触发的记忆",
  "daily_theme": "今日主题"
}
```

## 3. 第一版目标

第一版先验证下面几件事：

- 浏览体验是否像一本温柔的手账
- 女友角色是否有“活着”的感觉
- 日记 + 语音 + 图片 + 记忆是否能形成连续感
- Web 版的数据结构是否能复用到 iOS

## 4. 第一版范围

### 必做功能

- 首页卡片流
- 月份指示器
- 月历切换入口
- 日记卡片展示
- 日记详情展开
- 写日记页
- 语音留言页
- 设置页
- 本地假数据
- 本地存储
- 心情标签
- 图片展示
- 语音稿展示

### 保留功能

- AI 日记自动生成
- 语音留言自动生成
- 记忆召回
- 双 Agent 编排
- iOS 版本复用

### 暂缓功能

- 真正的对话功能
- 云端同步
- 登录注册
- 外部平台发送
- Android 支持

## 5. 内容生成规则

### 每日日记

- 至少每天一篇
- 由女友 Agent 生成
- 需要结合记忆、日期、情绪、关系阶段

### 语音留言

- 每天 2-3 条
- 不设固定时间点强绑定
- 完全由女友根据当天内容自由发挥
- 语音内容要和日记保持同一情绪线

### 记忆召回

翻看手账时，女友会自然切换以下模式：

- 感叹回忆模式
- 联结当下模式
- 调皮打趣模式

要求：

- 不能凭空捏造未发生过的事情
- 不能每篇都一个味道
- 不能过度甜腻

## 6. 页面结构

### 首页

推荐使用「卡片流为主，月历为辅」。

内容：

- 顶部月份指示器
- 日记卡片流
- 点击卡片看详情
- 右下角写日记按钮

### 写日记页

内容：

- 日期选择
- 文本输入
- 心情标签选择
- 图片占位 / 添加
- 语音留言预览
- 保存按钮

### 语音页

内容：

- 播放器
- 波形展示
- 文字稿
- 上下条切换

### 设置页

内容：

- 提醒时间
- 语音风格
- 导出方式
- 关于

### 月历页

作为首页的切换视图，不单独抢主流程。

内容：

- 月历网格
- 有日记的日期标记
- 点击日期跳转到对应卡片

## 7. UI 方向

### 视觉语言

- 奶油色系
- 日式手账风
- 轻纸张质感
- 温柔但不甜腻

### 配色

- 背景：`#FFF9F0`
- 卡片：`#FFFDF8`
- 主色：`#F8B4B4`
- 辅助色：`#A8D5BA`
- 文字：`#4A4A4A`
- 次级文字：`#8B8B8B`
- 高亮：`#FFD700`

### 排版

- 中文优先手写感字体
- 左对齐
- 留白自然
- 卡片圆角 8-12px
- 扁平化，少阴影

## 8. 数据模型

### Journal

```ts
type Journal = {
  id: string;
  date: string;
  weekday: string;
  mood: "开心" | "想念" | "感动" | "平静" | "调皮";
  content: string;
  images?: string[];
  voiceMessages: VoiceMessage[];
};
```

### VoiceMessage

```ts
type VoiceMessage = {
  id: string;
  timing: "morning" | "afternoon" | "night";
  transcript: string;
  duration: string;
};
```

### Preferences

```ts
type Preferences = {
  reminderTime: string;
  voiceStyle: "soft" | "warm" | "playful";
  exportMode: "pdf" | "image" | "none";
};
```

## 9. 技术落地建议

### 第一阶段

- Web 前端
- 本地 mock 数据
- 本地存储
- 页面流转和 UI 完成

### 第二阶段

- 接入真实 LLM
- 接入记忆存储
- 接入语音生成
- 补充自动生成逻辑

### 第三阶段

- iOS 版本
- 复用同一套数据结构和内容模型
- 保持产品体验一致

## 10. 设计决策

### 首页结构

推荐：卡片流为主，月历为辅。

理由：

- 更像手账
- 更符合“翻看”的体验
- Web 上更容易先做出氛围

### 语音时间点

推荐：不固定时间点。

理由：

- 更自然
- 更像真人
- 不会机械化

### 图片模式

保留两种：

- 日记配图
- 女友主动分享的“拍给你看”

理由：

- 配图满足手账装饰
- 主动分享增强陪伴感

## 11. 非目标

第一版不做：

- 复杂社交
- 多人关系系统
- 云端协作
- 登录体系
- 大型内容社区

## 12. 结论

这两个文件可以合并为一个统一版本：

- `proposal.md` 提供产品能力边界和技术方向
- `design.md` 提供界面、交互和体验细节

合并后，第一版最适合先做 Web 原型，再逐步接入真实生成、记忆和语音能力，最后迁移到 iOS。

