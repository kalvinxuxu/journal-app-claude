# Product Gap

日期：2026-05-13

依据：

- `SPEC.md`
- `DEV.md`
- 当前 `src/` 实现

目标：整理“根据产品规划，目前还需要开发什么功能”。

---

## 结论

如果按 `DEV.md` 的第一版原型目标看，主体页面和基础流转已经基本完成。

如果按 `SPEC.md` 的统一产品规划看，当前还缺少的主要是：

- 真实生成能力的稳定落地
- 本地内容与媒体的连续积累
- 设置项真正生效
- 语音交互补全
- 记忆能力持久化
- 更完整的女友角色机制

---

## P0：优先做

这些功能直接影响产品是否“真的能用”。

### 1. 修复本地内容 / 图片“看起来丢失”的连续性问题

现状：

- 日记、自拍、参考图只存于浏览器 `localStorage`
- 当前没有任何跨 origin / 跨端口迁移机制
- 一旦开发时访问地址变化，用户会看到“昨天生成过，今天没了”
- `loadJournals()` 在没有本地数据时会直接回退到 `mockJournals`

涉及文件：

- `src/services/memory.ts`
- `src/App.tsx`
- `src/data/mockJournals.ts`

目标：

- 明确区分“真实本地数据为空”与“回退到 mock 数据”
- 避免因为开发端口 / origin 变化造成内容表面消失
- 给出可见的数据状态提示或迁移策略

### 2. 修复并稳定图片生成链路

现状：

- 图片生成接口已经接入
- 但当前仍存在“无法正确持续生成图片”的问题
- 自拍一致性方案和实际接口能力也不完全匹配

涉及文件：

- `src/services/minimax.ts`
- `src/services/api/mediaClient.ts`
- `src/pages/WritePage.tsx`
- `src/pages/SettingsPage.tsx`
- `src/App.tsx`
- `review.md`

目标：

- 日记配图可稳定生成
- 自拍图可稳定生成
- 失败时有明确提示
- 不再把“没有生成”伪装成“功能已完成”

### 3. 把真实生成能力从“原型占位”推进到“产品可用”

现状：

- 日记内容主要仍来自本地模板/组合器
- 语音稿也是本地模板生成
- 只有媒体层部分接了真实接口
- 首页自动生成的“今日女友日记”实际上只会生成自拍，正文仍是固定文案，`voiceMessages` 为空

涉及文件：

- `src/services/journalGeneration.ts`
- `src/services/api/contentClient.ts`
- `src/services/generator.ts`
- `src/App.tsx`

目标：

- 日记内容生成接入真实 LLM
- 语音留言文本不再只是固定模板
- 内容和当天情绪、日期、记忆保持一致
- 自动生成链路不再只有自拍，没有正文和语音内容

### 4. 补齐真实记忆存储

现状：

- 当前有 memory engine
- 保存日记时会写入单例内存
- 但刷新页面后，记忆上下文并不会完整从历史日记恢复

涉及文件：

- `src/services/generator.ts`
- `src/services/memory.ts`
- `src/App.tsx`

目标：

- 应用启动时从本地历史日记重建记忆
- 后续真实接入远端记忆存储时，数据结构可复用

### 5. 收紧人物一致性策略

现状：

- 当前一致性主要依赖上一次返回的首张自拍 URL 作为 `referenceImage`
- `App` 自动生成链路只在“本地还没有 reference image”时保存一次，后续不会持续更新
- `SettingsPage` 重新生成形象会清空 reference image，导致角色形象重新洗牌

涉及文件：

- `src/services/api/mediaClient.ts`
- `src/services/minimax.ts`
- `src/services/memory.ts`
- `src/pages/SettingsPage.tsx`
- `src/App.tsx`

目标：

- 定义清楚“角色主参考图”的生命周期
- 区分“角色基准形象”与“某次自拍结果”
- 减少每天看起来像“类似的人”，而不是“同一个人”

---

## P1：第二优先级

这些功能不一定阻断主流程，但会明显影响完整度。

### 4. 让设置页选项真正生效

现状：

- `reminderTime`、`voiceStyle`、`exportMode` 目前只有表单状态
- 没有被实际业务消费

涉及文件：

- `src/pages/SettingsPage.tsx`
- `src/types/journal.ts`

需要补的功能：

- 提醒时间实际生效
- 语音风格影响语音生成结果
- 导出方式真正触发导出逻辑

### 5. 实现导出手账功能

现状：

- 设置页里已有导出方式
- 但没有真正导出 PDF / 图片

规划来源：

- `SPEC.md`
- `DEV.md`

目标：

- 支持导出当前手账或某篇日记
- 至少实现一种真实导出路径

### 6. 补语音页“上一条 / 下一条切换”

现状：

- 语音页只播放当前日记第一条语音
- 其余语音只展示文字稿

涉及文件：

- `src/pages/VoicePage.tsx`
- `src/components/VoicePlayer.tsx`

目标：

- 支持在同一篇日记的多条语音之间切换
- 让 2-3 条留言真正成为一组可浏览内容

### 7. 补设置页“关于”

现状：

- 规划中有“关于”
- 当前设置页没有这一块

涉及文件：

- `src/pages/SettingsPage.tsx`

目标：

- 增加产品说明 / 版本信息 / 数据说明等基础内容

---

## P2：增强项

这些更偏产品深化，不一定要立刻做。

### 8. 完整化“女友主动分享自拍”机制

现状：

- 已有 `selfies` 字段和生成入口
- 但生成时机、展示规则、失败回退、与 reference image 的关系还不完整

涉及文件：

- `src/services/minimax.ts`
- `src/App.tsx`
- `src/components/JournalCard.tsx`

目标：

- 明确“什么时候自动发自拍”
- 和日记内容形成联动
- 失败时体验自然，不突兀

### 9. 双 Agent 编排

现状：

- `SPEC.md` 中定义了“女友 Agent / 恋爱军师 Agent”
- 当前实现中还没有对应编排层

规划来源：

- `SPEC.md`

目标：

- 女友 Agent 负责内容表达
- 军师 Agent 负责润色、节奏控制、关系推进约束

### 10. 更完整的记忆召回模式

现状：

- 已有基础 memory engine
- 但还没有把“感叹回忆 / 联结当下 / 调皮打趣”做成稳定可控的内容策略

规划来源：

- `SPEC.md`

目标：

- 不同情绪和日期下，召回语气更自然
- 避免每篇一个味道

### 11. iOS 复用准备

现状：

- 数据模型已经比较轻
- 但还没有专门做跨端接口边界整理

规划来源：

- `SPEC.md`
- `DEV.md`

目标：

- 抽清楚“内容模型 / 存储模型 / 媒体模型”
- 为 iOS 复用做准备

---

## 已完成或基本完成

这些目前不算主要缺口：

- 首页卡片流
- 首页月历切换
- 日记详情展开
- 写日记页
- 语音页基础展示
- 设置页基础表单
- 本地 mock 数据
- 本地存储
- 心情标签
- 图片展示
- 语音稿展示

说明：

- “月历页”虽然不是独立路由，但已作为首页切换视图实现，和 `SPEC.md` 的设计方向一致。

---

## 推荐开发顺序

1. 图片生成链路修复
2. 真实 LLM / TTS / 记忆存储闭环
3. 设置项生效（提醒 / 导出 / 语音风格）
4. 语音页切换与设置页“关于”
5. 自拍机制深化
6. 双 Agent 编排
7. iOS 复用准备
